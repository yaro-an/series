{
  "name": "serial-series-app",
  "version": "1.0.0",
  "private": true,
  "description": "Personal series catalog with users, roles, posters, and details.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
